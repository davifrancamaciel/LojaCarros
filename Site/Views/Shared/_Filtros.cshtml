

@*<div class="filtros">
    <div class="container">
        <div class="col-md-10 dropdown-buttons">

            <div class="col-md-4 dropdown-button">
                <div class="section_room">
                    <select name="tipo" id="tipo" class="frm-field" required>
                        <option value="0"> - TIPO - </option>
                        @foreach (var item in ViewBag.Tipo)
                        {
                            <option value="@item.Nome">@item.Nome</option>
                        }
                    </select>
                </div>
            </div>
            <div class="col-md-4 dropdown-button">
                <div class="section_room">
                    <select name="marca" id="marca" class="frm-field">
                        <option value="0"> - MARCA - </option>
                        @foreach (var item in ViewBag.Marca)
                        {
                            if (Request.QueryString["marca"] != null)
                            {
                                if (@item.Nome.ToLower() == Convert.ToString(Request.QueryString["marca"]))
                                {
                                    <option selected value="@item.Nome">@item.Nome</option>
                                }
                            }
                            else
                            {
                                <option value="@item.Nome">@item.Nome</option>
                            }
                        }
                    </select>
                </div>
            </div>

            <div class="col-md-2 dropdown-button">
                <div class="section_room">
                    <select name="anoInicio" id="anoInicio" class="frm-fiel">
                        <option value=""> - ANO DE - </option>
                        @foreach (var item in ViewBag.AnoIncio)
                        {
                            if (Request.QueryString["anoInicio"] != null)
                            {
                                if (@item.AnoLista == Convert.ToInt32(Request.QueryString["anoInicio"]))
                                {
                                    <option selected value="@item.AnoLista">@item.AnoLista</option>
                                }
                            }
                            else
                            {
                                <option value="@item.AnoLista">@item.AnoLista</option>
                            }
                        }
                    </select>
                </div>
            </div>
            <div class="col-md-2 dropdown-button">
                <div class="section_room">
                    <select name="anoFim" id="anoFim" class="frm-fiel">
                        <option value=""> - ATE ANO - </option>
                        @foreach (var item in ViewBag.AnoFim)
                        {
                            if (Request.QueryString["anoFim"] != null)
                            {
                                if (@item.AnoLista == Convert.ToInt32(Request.QueryString["anoFim"]))
                                {
                                    <option selected value="@item.AnoLista">@item.AnoLista</option>
                                }
                            }
                            else
                            {
                                <option value="@item.AnoLista">@item.AnoLista</option>
                            }
                        }
                    </select>
                </div>
            </div>

        </div>

        <div class="col-md-2 submit_button">

            <input type="submit" value="Buscar" id="btnBuscar">

        </div>

        <div class="clearfix"> </div>
    </div>
</div>*@


<div class="filtros">
        <div class="container">
            <div class="col-md-10 dropdown-buttons">

                <div class="col-md-4 dropdown-button">
                    <div class="section_room">
                        <select name="tipo" id="tipo" class="frm-field" required>
                            <option value="0"> - TIPO - </option>
                            @foreach (var item in ViewBag.Tipo)
                            {
                                <option value="@item.Nome">@item.Nome</option>
                            }
                        </select>
                    </div>
                </div>
                <div class="col-md-4 dropdown-button">
                    <div class="section_room">
                        <select name="marca" id="marca" class="frm-field">
                            <option value="0"> - MARCA - </option>
                        </select>
                    </div>
                </div>

                <div class="col-md-2 dropdown-button">
                    <div class="section_room">
                        <select name="anoInicio" id="anoInicio" class="frm-fiel">
                            <option value=""> - ANO DE - </option>
                        </select>
                    </div>
                </div>
                <div class="col-md-2 dropdown-button">
                    <div class="section_room">
                        <select name="anoFim" id="anoFim" class="frm-fiel">
                            <option value=""> - ATE ANO - </option>
                        </select>
                    </div>
                </div>

            </div>

            <div class="col-md-2 submit_button">

                <input type="submit" value="Buscar" id="btnBuscar">

            </div>

            <div class="clearfix"> </div>
        </div>
    </div>
